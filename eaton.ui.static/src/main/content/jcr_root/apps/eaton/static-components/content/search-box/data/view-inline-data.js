"use strict";

use(function () {
    var data = {};
    data.headline = 'Product Search';
    data.form = {
      'placeholder': 'Products, sku, parts, specs',
      'submitLabel': 'Search'
    }
    return data;
});
